# core
torch        # Colab usually has CUDA-enabled torch preinstalled
gsplat
pycolmap
opencv-python
numpy
tqdm
imageio[ffmpeg]
# VGGT: PyPI is not always available, so install from GitHub in the notebook (see below)
# nerfstudio (optional; large)
# nerfstudio[all]