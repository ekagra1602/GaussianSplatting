# core
imageio[ffmpeg]
gsplat>=1.5
pycolmap>=3.12
torch>=2.5
torchvision>=0.20
torchaudio>=2.5
opencv-python-headless
numpy
imageio
tqdm
git+https://github.com/facebookresearch/segment-anything-2.git
# VGGT: PyPI is not always available, so install from GitHub in the notebook (see below)
# nerfstudio[all]